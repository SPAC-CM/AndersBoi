name: Secret slurper

on:
  push:
    branches:
      - '*'

jobs:
  GetSecret:
    runs-on: ubuntu-latest
    
    steps:
      - shell: bash
        name: Secret nom
        env:
          Secret : ${{secrets.DUMMY_THICC_BOI}}
        run: echo $Secret
      
