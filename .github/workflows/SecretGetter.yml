name: Secret slurper

on:
  push:
    branches:
      - '*'

jobs:
  GetSecret:
    runs-on: ubuntu-latest
    
    steps:
      - name: Setup python
        uses: actions/setup-python@v5
      - name: Secret nom
        env:
          DUMMY_THICC_BOI:  ${{secrets.DUMMY_THICC_BOI}}
        run: |
          ls
          python .\test.py

      
